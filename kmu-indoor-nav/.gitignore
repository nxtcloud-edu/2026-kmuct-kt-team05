# 실외 프로토타입 생성물 (fetch_osm.py / build_graph.py 로 재생성)
data/raw_osm.json
data/graph.json
data/graph.geojson
data/routes.geojson

# 국민대 공식 도면 원본. 이용 조건(usage_terms)이 확인되지 않아 커밋하지 않는다.
data/raw/floorplans/

# 팀원 그래프 복사본. 통합 저장소에서는 저장소 루트 data/ 를 직접 읽는다.
data/external/

__pycache__/
*.pyc
.pytest_cache/
*.log

# 주의: data/published/campus_v1.json 은 의도적으로 커밋한다.
# 배포 담당자가 클론만 하고 바로 실행할 수 있어야 하기 때문이다.
# 데이터를 바꾸면 아래를 다시 돌리고 커밋해야 한다.
#   python scripts/import_campus_part.py
#   python scripts/merge_graphs.py
#   python scripts/add_stepfree_bypasses.py