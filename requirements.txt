# 런타임 의존성 없음.
#
# 이 서버는 Python 표준 라이브러리만 쓴다 (http.server, json, pathlib).
# 이 파일은 AWS(Elastic Beanstalk / App Runner 등)가 저장소를 Python 앱으로
# 인식하게 하려고 둔다. 비어 있는 것이 정상이다.
#
# 아래는 '데이터를 다시 만들 때'만 필요하다. 배포 실행에는 필요 없다.
#   Pillow    도면 이미지에서 그래프를 만들 때 (scripts/build_from_navmap.py)
#   requests  OSM/도면 수집 (fetch_osm.py, scripts/collect_floorplans.py)
