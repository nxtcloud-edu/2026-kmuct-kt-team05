# P0-1 기준 재현 감사 (baseline audit)

작성: 2026-09-20 · 대상: `C:\Users\User\campus-nav`

## 저장소 상태

| 항목 | 결과 |
|---|---|
| git 저장소 | **없음** (`git rev-parse` → `fatal: not a git repository`) |
| 에이전트 지침 파일 | 없음 (`AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `README.md` 모두 부재) |
| 기존 파일 | `build_graph.py`, `config.py`, `diagnose.py`, `fetch_osm.py`, `routing.py`, `sweep.py`, `verify.py` + `data/` 4개 |
| 파일 타임스탬프 | 전부 2026-09-20 09:54 ~ 10:02 (같은 세션 산출물) |

보존 조치: 기존 최상위 파일과 `data/graph.json` 등을 **수정하지 않았다.** 신규 구조
(`backend/`, `scripts/`, `tests/`, `tools/`, `docs/`, `data/sources|raw|published|work`)를
병행 추가하는 방식으로 진행했다.

## 기준값 재현

실행: `python diagnose.py` (읽기 전용, 기존 `data/graph.json` 사용)

| 지표 | 지시서 기준 | 재현 결과 | 일치 |
|---|---|---|---|
| 노드 | 475 | 475 | O |
| 엣지 | 485 | 485 | O |
| 계단 엣지 | 25 | 25 | O |
| 연결요소 수 | 7 | 7 | O |
| 연결요소 크기 | `[157,117,110,53,17,13,8]` | `[157,117,110,53,17,13,8]` | O |

컴포넌트별 순환수(circuit rank = E−V+1): `16, 1, 0, 0, 0, 0, 0` / 전체 독립 순환 17.

`data/graph.json` 메타: `dem_dataset=aster30m`, `interpolation=bilinear`,
bbox `[37.6088, 126.9918, 37.6143, 127.0002]`.

**차이 없음. 변경 원인 보고 사항 없음.**

## 기존 산출물의 성격 재분류

지시서 3절의 지적을 반영해, 이전 세션의 결과를 아래와 같이 재규정한다.

| 이전 표현 | 수정된 규정 |
|---|---|
| "계단 정보는 OSM 확정값" | **OSM 태그 기반**. 현장 검증과 다를 수 있음 |
| `grade = dz / max(length, 15)` 의 결과를 "경사" | **분모 보정된 추정치**. 실측 경사가 아니며 사용자에게 경사로 표시 금지 |
| "휠체어 경로 687m, 최대경사 28.6%" | 기존 데이터·비용함수의 **출력**. 휠체어 통행 가능성을 증명하지 않음 |
| 건물 중심점 → 최근접 보행노드 자동 연결 | **가상 연결**. 시제품에만 보존, 사용자용 그래프(`data/published/`)에 미포함 |
| "프로파일별 경로 분기 = 성공" | 폐기. 성공 기준은 제약 충족·실제 연결·정답 일치 |
| "선형 경사 페널티는 언제나 무의미" | 과잉 일반화. 단조 오르막 등 제한 조건에서만 성립. 2차 페널티는 **선호 모델 선택지**이며 접근성 근거가 아님 |
| "고도차 상위 6쌍 검증" | 표본 편향. 고정 OD·양방향·평지·짧은 이동·층간으로 확장 필요 |
| "계단 제거 시 단절 건물 = 북악관/종합복지관/경상관" | 최대 컴포넌트 비교 방식의 산출물. 계단 제거 후 최대 요소가 바뀌면 설명이 틀릴 수 있어 **고정 출발점 기준 도달성으로 재계산 필요** (미수행) |
| bbox 내 건물 전부가 목적지 후보 | 캠퍼스 외 건물(로얄하이츠 빌라, 플러스빌 등) 포함. 공식 건물 목록 기반 등록으로 교체 필요 |

## 현재 평가

기존 실외 자산의 상태는 **"실외 그래프·탐색 개념 검증"** 이다. 실내 정보, 시간 추정,
현장 접근성 검증이 없으므로 서비스 완료로 보고하지 않는다.

## 이번 작업에서 추가한 것

- `backend/app/models/schema.py` — 근거 동반 스키마 (`Attr`, `Verification`)
- `backend/app/graph/dataset.py` — 방향 다중그래프 로더 + 구조 검증
- `backend/app/routing/policy.py` — 하드 제약 / 시간모델 / 선호비용 **분리**
- `backend/app/routing/engine.py` — Dijkstra 기준 구현, 상태 5종 구분
- `tests/` — 합성 그래프 29개 테스트 (전부 통과)
- `data/published/mirae_indoor_v1.json` — 미래관 3층 실도면 기반 실내 그래프
- `tools/editor/index.html` — 최소 도면 편집기

기존 `routing.py` / `verify.py` / `sweep.py` 는 **이관하지 않고 그대로 보존**했다.
신규 엔진은 별도 경로(`backend/app/routing/`)에 있으며 기존 코드를 import 하지 않는다.
